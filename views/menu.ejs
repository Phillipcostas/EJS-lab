<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<body>
  <h1>Full Menu</h1>
  <ul>
    <% menu.forEach((item) => { %>
        <a href="/<%= item %>">
      <li><%= item.name %>: 
        <br/>
        <%= item.price %>: 
        <br/>
        <%= item.rating %>: 
        <br/> 
        <%= item.details %></li>
    </a>
    <% }); %>
  </ul>
</body>
</html>
